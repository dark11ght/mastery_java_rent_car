spring:
  application:
    name: SpringApplication
  datasource:
    driverClassName: org.postgresql.Driver
    url: "jdbc:postgresql://localhost:5432/rentcar?autoreconnect=true"
    password: "root"
    username: "postgres"
  flyway:
    enabled: true
    validate-on-migrate: true
